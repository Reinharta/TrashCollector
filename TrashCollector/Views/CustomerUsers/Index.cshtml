@model TrashCollector.ViewModels.CustomerUsersIndexViewModel

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Customer Home</h2>

<p>
    @Html.ActionLink("Add A Pick-Up", "Create", "PickUps", Model.Customer.CustomerId, null)
</p>
<table class="table">
    <tr>
        <th>
            Pick-Up Day
        </th>
        <th>
            Start Date
        </th>
        <th>
            End date
        </th>
        <th>
            Recurring
        </th>
    </tr>

    @foreach (var pickup in Model)
    {
    <tr>
        <td>
            @pickup.PickUpDay
        </td>
        <td>
            @pickup.StartDate
        </td>
        <td>
            @pickup.EndDate
        </td>
        <td>
            @pickup.Recurring
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", "PickUps", new { id = pickup.PickUpId }, null) |
            @Html.ActionLink("Details", "Details", new { id = pickup.PickUpId }, null) |
            @Html.ActionLink("Delete", "Delete", new { id = pickup.PickUpId }, null)
        </td>
    </tr>
    }

</table>

